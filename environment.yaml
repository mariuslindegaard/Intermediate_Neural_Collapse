# Command to build envirnoment: conda env create -f this_file.yaml
name: nc
channels:
  - pytorch
  - defaults
  - conda-forge
dependencies:
  - numpy>=1.21.6
  - pandas>=1.2.5
  - pytorch>=1.11.0
  - scipy>=1.7.3
  - tensorboard>=2.9.0
  - torchvision>=0.12.0
  - tqdm>=4.64.0
  - pyyaml>=6.0
  - tensorly>=0.8.0
  - seaborn>=0.12.2
  - PIL
#   - python=3.7
# prefix: /om2/user/lindegrd/miniconda3/envs/nc
